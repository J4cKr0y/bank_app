#ifndef SEARCH_H
#define SEARCH_H

/* Fonction de recherche binaire.
- cherche 'target' (la valeur à trouver) dans le tableau 'arr'.
Le tableau DOIT être trié pour que cette fonction fonctionne. */
int binary_search(int arr[], int low, int high, int target);

#endif // SEARCH_H
